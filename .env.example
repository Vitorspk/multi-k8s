# Example Environment Variables for Secret Manager Setup
# Copy this file to .env.local and update with your values
# These values will be used by the manage-secrets.sh script

# GCP Configuration
PROJECT_ID=vschiavo-home
REGION=southamerica-east1

# Database Configuration (these values will be stored in Secret Manager)
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_USER=postgres
POSTGRES_HOST=postgres-cluster-ip-service
POSTGRES_PORT=5432
POSTGRES_DATABASE=postgres

# Redis Configuration (these values will be stored in Secret Manager)
REDIS_HOST=redis-cluster-ip-service
REDIS_PORT=6379

# Kubernetes Configuration
GKE_CLUSTER_NAME=multi-k8s-cluster
GKE_ZONE=southamerica-east1-a

# Notes:
# - Never commit actual secrets to version control
# - Use strong, unique passwords for production
# - Rotate passwords regularly
# - Monitor access through GCP Cloud Logging